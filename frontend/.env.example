# Base URL of the ASP.NET Core Web API (no trailing slash).
# This is NOT a secret, never put JWT signing keys, DB credentials,
# or other backend secrets in a frontend env variable.
VITE_API_BASE_URL=http://localhost:3001
